git status
git init
git add .
git commit -m "-----"
git config user.name "----"
git config user.email "-----"
git config --global user.name "----"
git config --global user.email "-----"
git log
git remote set-url origin https://github.com/rishu89/automatedtimetable.git
git push -u origin master
git gc // remove previous commits 
 